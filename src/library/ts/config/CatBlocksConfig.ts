export interface CatBlocksConfig {
  advancedMode: boolean;
  container: string;
  renderSize: number;
  shareRoot: string;
  media: string;
  language: string;
  rtl: boolean;
  i18n: string;
  noImageFound: string;
  renderScripts: boolean;
  renderLooks: boolean;
  renderSounds: boolean;
  readOnly: boolean;
}
